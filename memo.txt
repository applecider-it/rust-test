
cd ~/workspace/github/rust-test/src

-----------------------------------

port

rust: 3000

-----------------------------------

サーバー起動

cargo-watch -x "run --bin server"

cargo run --bin server

-----------------------------------

CLI起動

cargo run --bin cli

-----------------------------------


http://localhost:3000


-----------------------------------

セットアップ


cargo build

-----------------------------------

エラーチェック

cargo check

-----------------------------------

ビルド

cargo build --release

./target/release/server

./target/release/cli


-----------------------------------

Wasm

cd ~/workspace/github/rust-test/wasm

ビルド

cargo watch -s "wasm-pack build --target web"

wasm-pack build --target web

動作確認

python3 -m http.server 8000

http://localhost:8000/

キャッシュが残るので、更新後は、スーパーリロードが必要

